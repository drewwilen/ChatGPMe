[project]
name = "chatgpme-backend"
version = "0.1.0"
description = "Backend-first MVP scaffolding for ChatGPMe"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
